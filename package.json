{
    "name": "multimodal-rag-app-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev:frontend": "cd app && npm run dev",
        "dev:backend": "cd api && .venv/bin/uvicorn index:app --reload --port 8000",
        "dev": "npm-run-all --parallel dev:frontend dev:backend"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}